apiVersion: apps/v1
kind: Deployment
metadata:
  name: encuestapp
  labels:
    app: encuestapp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: encuestapp
  template:
    metadata:
      labels:
        app: encuestapp
    spec:
      containers:
        - name: encuestapp
          image: 127.0.0.1:82/encuestapp-prueba/encuestapp:a6ae4f7
          ports:
            - containerPort: 80
